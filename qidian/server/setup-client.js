document.getElementById('setupForm').onsubmit=async e=>{e.preventDefault();const input=document.getElementById('apiKey'),button=document.getElementById('saveKey'),status=document.getElementById('setupResult');button.disabled=true;status.textContent='正在向 DeepSeek 验证…';try{const r=await fetch('/api/local-config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({apiKey:input.value.trim()})});const d=await r.json();if(!r.ok)throw Error(d.error);input.value='';status.textContent='✓ 已安全保存。返回学习应用即可开始真实问答与识题。';}catch(e){status.textContent=e.message||'保存失败，请重试。';}finally{button.disabled=false;}};
